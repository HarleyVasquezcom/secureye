{
  "name": "secureye-cctv",
  "version": "1.1.1",
  "private": true,
  "description": "SECUREYE CCTV site — static build, serve and screenshot pipeline.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "opt": "node scripts/optimize-assets.mjs",
    "serve": "node scripts/serve.mjs",
    "shots": "node scripts/shots.mjs",
    "dl": "node scripts/download-assets.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "sharp": "^0.35.4"
  }
}
